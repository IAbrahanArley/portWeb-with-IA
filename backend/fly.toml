app = "nest-api-portfolio"

[env]
  NODE_ENV = "production"
  PORT = "3000"
  DATABASE_URL = "postgresql://usuario:senha@aws-0-us-west-1.pooler.supabase.com:6543/nomedb?schema=public"

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 3000
  force_https = true

  [[http_service.ports]]
    handlers = ["http"]
    port = 80

  [[http_service.ports]]
    handlers = ["tls", "http"]
    port = 443
